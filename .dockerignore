node_modules
npm-debug.log
# Runtime env vars must be injected at runtime (e.g., Docker/K8s env or secrets), not baked into the image.
# Example required ENV: PORT, DATABASE_URL
# Note: NODE_ENV is intentionally set in the Dockerfile at build time.
.env
.DS_Store
coverage
.git

